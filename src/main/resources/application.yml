spring:
  application:
    name: Conference_Management
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:  jdbc:mysql://localhost:3306/conference_manage?useSSL=false&serverTimezone=UTC
    username: root
    password:

mybatis:
  mapper-locations: classpath:mapper/*.xml
